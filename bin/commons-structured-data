#!/usr/bin/env perl

use strict;
use warnings;

use App::Commons::StructuredData;

our $VERSION = 0.01;

# Run.
exit App::Commons::StructuredData->new->run;

# Print version.
sub VERSION_MESSAGE {
	print $VERSION."\n";
	exit 0;
}

__END__
